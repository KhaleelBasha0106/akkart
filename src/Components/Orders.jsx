import React from "react";
import { Box,Typography } from "@mui/material";
const Orders = () => {
  return (
    <Box sx={{ marginTop: 20 }}>
      <Typography variant="h5" textAlign={"center"}>
        Need to Work On this Order Component
      </Typography>
    </Box>
  );
};

export default Orders;
