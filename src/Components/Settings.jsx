import { Box, Typography } from "@mui/material";
import React from "react";

const Settings = () => {
  return (
    <Box sx={{ marginTop: 20 }}>
      <Typography variant="h5" textAlign={"center"}>
        Need to Work On this Settings Component
      </Typography>
    </Box>
  );
};

export default Settings;
